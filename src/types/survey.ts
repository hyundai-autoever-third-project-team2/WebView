export interface IServeyData {
  min_price: number;
  max_price: number;
  min_distance: number;
  max_distance: number;
  min_model_year: number;
  max_model_year: number;
  car_model_ids: number[];
  colors: number[];
}
